{
  "name": "uroa-stem",
  "private": true,
  "scripts": {
    "build:all": "git submodule foreach 'npm run build'",
    "install:all": "git submodule foreach 'npm install'",
    "clean": "rm -rf dist/*"
  }
  ,
  "devDependencies": {
    "@types/katex": "^0.16.8",
    "@types/node": "^25.2.2",
    "madge": "^8.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vite-tsconfig-paths": "^6.1.0"
  },
  "dependencies": {
    "firebase": "^10.14.1",
    "katex": "^0.16.28"
  }
}
